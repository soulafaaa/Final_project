import csv
import sys 

class Recommender:
    def __init__(self)


def recommend_artist(Genres, type):
    """ This method will compare the argument added to the list of artist we have and will 
    recommend a new artist 
    
    Args: 
        Genre (list): valued representing the user favorite music type
        Type (String): value representing whether the user is looking for an artist orband 
    Return: 
        List of recomended artist
    """
    
    
def recommend_song(Genre,type):
    """ This method will compare teh arument passed to the existing list of songs we have 
    and will recommend diffrent songs
    Args: 
        Genre (list): values representing the user favorite music type
        Type (String): value representing whether the user is looking for an artist orband 
     Return: 
        List of recomended songs
    
    """  
    
def main():
    """ the main will collect user input and pass the responses as argument to the other methods.
    Args:
        None
        
    Return: 
        String:  with all the  recommnded music based on their 
    favorite atrist and genre they inputed.
    """
    open_file()
    
    
     

if __name__ =="__main__":
    main()
